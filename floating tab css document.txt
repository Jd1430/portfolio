/* General Section Styles */
.works-section {
    padding:0px;
    margin-top: 0px;
  }
  
  /* Sticky Header Tabs */
  .header-tabs-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: sticky;
    top: 0;  /* Stick to the top of the viewport when scrolled */
    background-color: #fff;  /* Background color to cover content below */
    z-index: 1000;  /* Ensures the tabs are always on top */
    padding: 5px 20px;
    box-shadow: 0px 2px 5px rgb(254, 253, 253);  /* Adds a slight shadow */
    border-radius: 10px;
  }
  
  /* Section Heading */
  .section-heading {
    font-size: 24px;
    color: #333;
    margin: 0;
    font-weight: bold;
  }
  
  /* Tab Buttons */
  .tabs {
    display: flex;
    gap: 10px;
  }
  
  .tab-button {
    padding: 10px 20px;
    border: none;
    background-color: #f1f1f1;
    cursor: pointer;
    font-size: 16px;
    border-radius: 100px;
    transition: background-color 0.3s ease;
  }
  
  .tab-button.active,
  .tab-button:hover {
    background: linear-gradient(135deg, #3a7bd5, #00d2ff);
    color: #fff;
  }